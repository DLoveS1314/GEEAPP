PORT=3001
FRONTEND_ORIGIN=http://localhost:5173
GEE_PROJECT_ID=my-project-94116-map
GEE_CREDENTIALS_PATH=../gee-service-account.json
GEE_PROXY=http://127.0.0.1:7890
GEE_PROXY_INSECURE=1

# PostgreSQL / PostGIS database used by the HexRemap dataset APIs.
# Copy this file to .env locally and replace credentials with real values.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/hexremap
PGSSL=false
